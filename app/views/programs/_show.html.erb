<div class="section">
  <h2> <%= @program.title %> </h2>
  <p> <%= @program.headline %> </p>
  <p> <%= @program.description %> </p>
  <p> <%= link_to 'Apply to ths program', new_program_application_path(@program, @application), class: 'btn btn-primary' %> </p>
  <%= link_to "like", toggle_favorite_program_path(program), remote: true, method: :post %>
</div>
